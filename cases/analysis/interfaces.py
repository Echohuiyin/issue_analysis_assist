"""Phase-3/4 reserved interfaces (implementation intentionally removed).

The project currently prioritizes phase-1/2 (acquisition + storage + RAG).
These classes remain as compatibility placeholders and will be implemented
in a later phase.
"""


class SKILLStorage:
    """Placeholder interface for future SKILL storage implementation."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError("SKILLStorage is reserved for future phase implementation.")


class SKILLTrainer:
    """Placeholder interface for future SKILL trainer implementation."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError("SKILLTrainer is reserved for future phase implementation.")


class IssueAnalyzer:
    """Placeholder interface for future issue analyzer implementation."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError("IssueAnalyzer is reserved for future phase implementation.")
